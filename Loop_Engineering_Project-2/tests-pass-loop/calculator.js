function add(a, b) {
  return a + b; // Fixed: correctly adds two numbers
}

function multiply(a, b) {
  return a * b; // Fixed: correctly multiplies two numbers
}

module.exports = { add, multiply };
